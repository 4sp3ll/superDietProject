const fiberNote = () => {
    return 'Note: not every product have information about fiber. For example if you choose min. fiber 5g, every product with empty fiber field will disappear from search results.'
}

export default fiberNote