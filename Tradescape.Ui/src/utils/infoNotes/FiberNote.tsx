import React, { ReactElement } from 'react'


const FiberNote = () => {

    return (
        <div>
            Note: only some percent of products contain information about fiber,
            all products without this information will be throw out from the result if you'll not leave these fields empty
        </div>
    )
}

export default FiberNote