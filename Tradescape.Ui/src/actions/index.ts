import {
    addCategory,
    removeCategory,
} from './categoriesActions'
import {
    minCarbo,
    minProtein,
    minFat,
    minSalt,
    maxSalt,
    minRoughage,
    maxRoughage,
    containWords
} from './filtersActions'

const allActions = {
    addCategory,
    removeCategory,
    minCarbo,
    minProtein,
    minFat,
    minSalt,
    maxSalt,
    minRoughage,
    maxRoughage,
    containWords
}

export default allActions